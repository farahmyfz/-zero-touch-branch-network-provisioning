name: NetDevOps Automation Pipeline

on:
  push:
    branches: [ main ]
  
  workflow_dispatch:
    inputs:
      target_router:
        description: 'Router Target (diambil dari Dashboard)'
        required: true
        default: 'R1'

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Ambil Kode Terbaru dari GitHub
        uses: actions/checkout@v4

      - name: Eksekusi Ansible setup_sistem.yml
        run: |
          export ANSIBLE_HOST_KEY_CHECKING=False
          
          ansible-playbook -i hosts.ini setup_sistem.yml \
            --limit "${{ github.event.inputs.target_router || 'all' }}" \
            -e "ansible_user=admin" \
            -e "ansible_password=admin"
